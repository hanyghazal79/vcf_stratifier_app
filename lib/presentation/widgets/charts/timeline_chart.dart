import 'package:flutter/material.dart';

class TimelineChart extends StatelessWidget {
  const TimelineChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Timeline Chart - Coming Soon'));
  }
}
