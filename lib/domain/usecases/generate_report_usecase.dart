import '../../data/models/analysis_result.dart';

class GenerateReportUseCase {
  Future<void> execute(AnalysisResult result, String format) async {
    throw UnimplementedError('Use PDFService directly');
  }
}
