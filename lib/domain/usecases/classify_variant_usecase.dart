import '../../data/models/genetic_variant.dart';

class ClassifyVariantUseCase {
  RiskLevel execute(GeneticVariant variant) {
    throw UnimplementedError('Use ClassificationService directly');
  }
}
